//sza101118-1725       
//sza101025
Процедура ПередЗаписью(Отказ)
	
	Если сокрлп(ЭтотОбъект.Комментарий) = "" Тогда
		ЭтотОбъект.Комментарий = ЭтотОбъект.Наименование;
	КонецЕсли;
	
	ЭтотОбъект.Наименование = СокрЛП(ЭтотОбъект.Наименование);
	
	Если НЕ ЭтотОбъект.Предопределенный Тогда
		ЭтотОбъект.Наименование = СтрЗаменить(ЭтотОбъект.Наименование, " ", "");
	КонецЕсли;
	
КонецПроцедуры

