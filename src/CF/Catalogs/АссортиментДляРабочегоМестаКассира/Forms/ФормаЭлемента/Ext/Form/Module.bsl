// sza140605-2335  
Процедура ОбновитьПримерКнопки()
	
	ЦветФона = Новый Цвет(Объект.ЦветФонаКнопкиКрасный, Объект.ЦветФонаКнопкиЗеленый, Объект.ЦветФонаКнопкиСиний);
	Элементы.ПримерКнопки.ЦветФона = ЦветФона;
	ЦветТекста = Новый Цвет(Объект.ЦветТекстаКнопкиКрасный, Объект.ЦветТекстаКнопкиЗеленый, Объект.ЦветТекстаКнопкиСиний);
	Элементы.ПримерКнопки.ЦветТекста = ЦветТекста;
	Если Объект.РазмерТекста = ПредопределенноеЗначение("Перечисление.ВидыРазмеров.Крупный") Тогда
		Элементы.ПримерКнопки.Шрифт.Размер = 16;
	ИначеЕсли Объект.РазмерТекста = ПредопределенноеЗначение("Перечисление.ВидыРазмеров.Мелкий") Тогда
		Элементы.ПримерКнопки.Шрифт.Размер = 8;
	ИначеЕсли Объект.РазмерТекста = ПредопределенноеЗначение("Перечисление.ВидыРазмеров.Огромный") Тогда
		Элементы.ПримерКнопки.Шрифт.Размер = 20;
	ИначеЕсли Объект.РазмерТекста = ПредопределенноеЗначение("Перечисление.ВидыРазмеров.Средний") Тогда
		Элементы.ПримерКнопки.Шрифт.Размер = 12;
	КонецЕсли;	
	
КонецПроцедуры

&НаКлиенте
Процедура ПриЗакрытии()
	ОбщийМодульКлиент.СобытиеФормы(ЭтаФорма, 1, Объект.Ссылка, ИСТИНА);
КонецПроцедуры

&НаКлиенте
Процедура ПриОткрытии(Отказ)
	
	ОбщийМодульКлиент.СобытиеФормы(ЭтаФорма, 0, Объект.Ссылка, ИСТИНА);
	ОбновитьПримерКнопки();
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Отказ = ОбщийМодульСервисСервер.ПроверитьОтказДоступа("000365", ЭтаФорма, Отказ, Объект);
	
	Если НЕ Отказ Тогда
		Если ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ИспользоватьСобственныйПереводЭлементовИнтерфейса") Тогда
			ОбщийМодульСервер.ПеревестиРеквизитыФормы(ЭтаФорма);	
		КонецЕсли;
		
		Если НЕ ЗначениеЗаполнено(Объект.Ссылка) Тогда
			
			Если ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ИспользоватьСложныйМеханизмЦен") 
				И НЕ ЗначениеЗаполнено(Объект.РеализоватьСИспользованиеОпределенногоВидаЦен) Тогда
				
				Объект.РеализоватьСИспользованиеОпределенногоВидаЦен = ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ВидЦенДляРеализацииНаРабочемМестеОператораКассы");
				Если НЕ ЗначениеЗаполнено(Объект.РеализоватьСИспользованиеОпределенногоВидаЦен) Тогда
					Объект.РеализоватьСИспользованиеОпределенногоВидаЦен = ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ВидЦенРасходованияПоУмолчанию");
				КонецЕсли;
			КонецЕсли;			
			
			Если ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ВестиУчетПоСкладам")
				И НЕ ЗначениеЗаполнено(Объект.РеализацияСОпределенногоСклада) Тогда
				
				Объект.РеализацияСОпределенногоСклада = ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("СкладПоУмолчанию");				
			КонецЕсли;
			
			Если ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ВестиУчетСобственныхЮридическихЛиц")
				И НЕ ЗначениеЗаполнено(Объект.РеализацияОтОпределеннойОрганизации) Тогда
				
				Объект.РеализацияОтОпределеннойОрганизации = ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ОрганизацияПоУмолчанию");
			КонецЕсли;
			
			Если ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ВестиУчетДенегВНесколькихХранилищах")
				И НЕ ЗначениеЗаполнено(Объект.РеализоватьСПоступлениеДенегВОпределенноеХранилище) Тогда
				
				Объект.РеализоватьСПоступлениеДенегВОпределенноеХранилище = ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ХранилищеДенегДляРабочегоМестаОператораКассы");
				Если НЕ ЗначениеЗаполнено(Объект.РеализоватьСПоступлениеДенегВОпределенноеХранилище) Тогда
					Объект.РеализоватьСПоступлениеДенегВОпределенноеХранилище = ОбщийМодульПовтор.ПолучитьЗначениеНастройкиИлиКонстанты("ОсновноеХранилищеДенег");
				КонецЕсли;
			КонецЕсли;
			
		КонецЕсли;
		
		Если ОбщийМодульПовтор.ПолучитьПараметрСеанса("НеМожетМенятьЦены") Тогда
			Элементы.РеализоватьСИспользованиеОпределенногоВидаЦен.Доступность = ЛОЖЬ;	
		КонецЕсли;	
		
		ОбщийМодульСервисСервер.ОформитьФорматКоличества(Элементы.Количество, ИСТИНА);	
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура РазмерТекстаПриИзменении(Элемент)
	ОбновитьПримерКнопки();
КонецПроцедуры

&НаКлиенте
Процедура ЦветТекстаКнопкиЗеленыйПриИзменении(Элемент)
	ОбновитьПримерКнопки();
КонецПроцедуры

&НаКлиенте
Процедура ЦветТекстаКнопкиКрасныйПриИзменении(Элемент)
	ОбновитьПримерКнопки();
КонецПроцедуры

&НаКлиенте
Процедура ЦветТекстаКнопкиСинийПриИзменении(Элемент)
	ОбновитьПримерКнопки();
КонецПроцедуры

&НаКлиенте
Процедура ЦветФонаКнопкиЗеленыйПриИзменении(Элемент)
	ОбновитьПримерКнопки();
КонецПроцедуры

&НаКлиенте
Процедура ЦветФонаКнопкиКрасныйПриИзменении(Элемент)
	ОбновитьПримерКнопки();
КонецПроцедуры

&НаКлиенте
Процедура ЦветФонаКнопкиСинийПриИзменении(Элемент)
	ОбновитьПримерКнопки();
КонецПроцедуры

&НаКлиенте
Процедура ПослеЗаписи(ПараметрыЗаписи)
	глЧислоОбъектов = глЧислоОбъектов + 1; глПроверятьСообщения = ИСТИНА;
КонецПроцедуры
